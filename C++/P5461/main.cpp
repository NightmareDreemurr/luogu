#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int sub_array_size = pow(2, n);
    vector<vector<int>> v(n, vector<int>(n, 1));

    while (sub_array_size)  {
        for(int i = 0; i < n; i++) {
            for(int j = 0; j < n; j++) {

            }
        }
    }
    return 0;
}
